#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num = '0';

	for (num = '0'; num < 10; num++)
		printf("%d\n", num);
	return (0);
}
